# Bitácora

**Append-only.** Una línea al final por cada cierre de ticket o hallazgo que cueste tiempo.
**Nunca se reescribe ni se reordena** — su valor es que el orden sea el real.

Formato: `AAAA-MM-DD · <ID o ámbito> · qué pasó · commit · qué se verificó · qué quedó abierto`.

Lo que **no** va aquí: conteos (eso lo deriva `scripts/estado-tickets.js`), narración de estado
actual (eso vive en el campo `Estado:` de cada ticket), ni prosa de handoff (el handoff es
desechable y vive fuera del repo).

---

- 2026-08-29 · planeación · cerrados `DOCUMENTO-FUNDACIONAL.md`, `PRODUCT.md`, `CONTEXT.md`, `PLAN-TICKETS.md`, ADR-0001 y ADR-0002. Prototipo `prototipo/ficha-proto.html` con datos reales: valida el hero partido con `object-fit: contain` y descarta el par 2-up de la referencia. Sin código todavía.
- 2026-08-30 · andamiaje · montado el carril secuencial del método (skill `metodo-tickets`): `docs/tickets/` con los 16 tickets derivados de `PLAN-TICKETS.md` §2, `scripts/estado-tickets.js` y `scripts/handoff.js`, esta bitácora, `docs/gotchas.md` y el `CLAUDE.md` del repo. Verificado: `node scripts/estado-tickets.js` sale con código 0 y 16/16 tickets con sus 10 campos. **No se montó el carril paralelo** (worktrees, rol integrador) — decisión explícita: 16 tickets con camino crítico casi lineal no lo justifican. Sigue abierto todo lo de `docs/tickets/README.md` § Bloqueos.
- 2026-08-30 · T-01 · bootstrap Next.js App Router + TS + Tailwind v4 + ESLint + Prettier, fuentes Cabinet Grotesk Display + Satoshi self-hosted (`next/font/local`, ADR-0002). Commit `5fbb095`. Verificado: `npm run dev` sirve `Santa Maria Performance Horses` en `/` (confirmado por contenido, no por código HTTP, y en navegador real vía chrome-devtools); `npm run build` genera `/` como estática; `.next/server/app` contiene el nombre del sitio. Baseline de lint fijado en `.lint-baseline.json` (4 errores heredados de `scripts/*.js`, ajenos a este ticket). **Queda abierto:** la URL de Vercel — el deploy lo hace César, no verificado.
- 2026-08-30 · T-16 · gate `npm run lint:baseline` (`scripts/lint-baseline.mjs`), compara ESLint (formatter `json`) contra `.lint-baseline.json` y falla solo si sube el total de errores, sube el de warnings, o aparece una regla nueva; nombra archivo y regla. Commit `a22ca93`. Verificado con la secuencia de rojo real: árbol limpio → código 0 (`4 errores (baseline 4), 0 warnings (baseline 0)`); error nuevo a propósito en `src/app/page.tsx` (variable sin usar) → código 1, mensaje nombró `src/app/page.tsx` y `@typescript-eslint/no-unused-vars`; revertido → código 0 otra vez y `git status` limpio. `npm run build` y `npm run lint` re-corridos contra el árbol final (commit `a22ca93`): build en 0, lint en 1 con los mismos 4 errores heredados de T-01. **No se agregaron tests de capa 1/2** (fuera de alcance, ver decisión del proyecto).
- 2026-08-30 · T-16 (deuda) · cerró los tres huecos declarados en el handoff sobre `lint:baseline`: rojo real con un error de verdad (`@typescript-eslint/no-explicit-any`, no un warning) → código 1; bajar el conteo (4→3 errores, deshabilitando un `no-require-imports` heredado) → código 0, no falla; normalización de rutas Windows confirmada — cruza bien y, además, el código de salida depende solo de conteos globales, no de las claves por archivo. Sin commit propio (nada cambió en el repo salvo la nota en `T-16.md`); árbol limpio verificado tras cada prueba. Se encontraron y guardaron en `git stash` (`restos de verificacion T-16 interrumpida`) cambios sin commitear de una sesión previa en `scripts/handoff.js` y `src/app/layout.tsx` — pertenecen a un intento de revisión matado a mitad de vuelo, no son trabajo del proyecto; **pendiente que César decida si el stash se descarta**. **Sigue abierto:** la decisión de la §3 del handoff (código 1 permanente de `estado-tickets.js` mientras T-01 espera el deploy) — es de César, no se tocó.
- 2026-08-30 · T-01 (cierre) · resuelto el deploy a Vercel (lo hizo César autorizando cada paso: cuenta nueva de GitHub `cesar32777`, `gh auth login` web, conexión GitHub↔Vercel, Vercel GitHub App). Repo empujado a `github.com/cesar32777/stamariahorses` (público), proyecto `stamariahorses` enlazado en Vercel, rama de producción `master`, deploy de `target: production` sobre el commit `358707e`. Verificado por HTTP: `https://stamariahorses-cesarmunizce32-8387.vercel.app` → 200 con `<title>` y `<h1>` = `Santa Maria Performance Horses`. Auto-deploy activo (cada push a `master` dispara build). Con esto se cierra la otra mitad del `Hecho cuando:` y T-01 pasa a ✅ (móvil y escritorio). `estado-tickets.js` vuelve a salir en 0.
- 2026-08-30 · T-02 · tipado y validación del dato en tiempo de build. `zod@4.5.4` como dependencia de producción. `src/data/schema.ts`: `FotoSchema`/`CaballoSchema`/`VendedorSchema`/`CatalogoSchema` con los tipos inferidos `Foto`/`Caballo`/`Catalogo` — campos y nombres de `CONTEXT.md` + DOCUMENTO-FUNDACIONAL §4, `nombre`/`estado`/`fotos` obligatorios y el resto nulable (RF5). `src/data/caballos.ts` corre `CatalogoSchema.safeParse` al evaluarse el módulo y hace `throw` con mensaje legible; se importa desde `src/app/page.tsx` (que consume `caballos` para que no sea import muerto), así `next build` lo ejecuta en "Collecting page data". Commit `f7505da`. Verificado por ejecución: quitado `nombre` al caballo `id 07` en `data/caballos.json` → `npm run build` falla con código 1 y `caballo id 07 → campo 'nombre': Invalid input: expected string, received undefined`; restaurado con `git checkout -- data/caballos.json` (JSON válido, `git status` limpio) → `npm run build` código 0. Segundo `next build` con `bucket:"16:9"` en una foto anidada → también falla código 1 y el mensaje nombra caballo + ruta (`caballo Caballo 05 → campo 'fotos.2.bucket'`). Además cada restricción del esquema ejercitada en rojo de a una (19 mutaciones vs `safeParse`: enums, `min(1)`, tipos equivocados, `int`, arrays vacíos, positivos, campos faltantes — 17 rotas + 2 controles positivos, todas con el resultado esperado; script desechable en scratchpad, no commiteado). El JSON roto no se commiteó en ningún momento. `npm run lint:baseline` código 0, `4 errores (baseline 4)` sin cambios. Sin tests de capa 1 en el repo (decisión del proyecto: el build es la red). Commit `3ab348e`.
- 2026-08-30 · T-03 · capa de acceso al Catálogo. `src/data/catalogo.ts`: `caballosPublicos` (filtra `estado:"retirado"`), `getCaballoPorSlug`, `slugsPublicos`, `slugDeNombre` (NFD + quita acentos + colapsa a guion; hoy `"Caballo 07"` → `caballo-07`). Guarda de build ante slug vacío o duplicado (misma filosofía que T-02). `src/app/caballos/[slug]/page.tsx`: ruta esqueleto — `generateStaticParams` = solo Disponibles, `dynamicParams = false`, `notFound()`; el cuerpo real de la Ficha es de T-08a. `page.tsx` toma el conteo de la capa. Commit `fa30b5c`. Verificado por ejecución: `caballo 07` → `retirado` en los datos → `npm run build` genera 17 páginas (no 18), no existe `.next/server/app/caballos/caballo-07.html`, y `next start` da 404 en `/caballos/caballo-07` y en `/caballos/no-existe`, 200 en `/caballos/caballo-01`, y la portada baja de 14 a 13; restaurado el JSON con `git checkout` → 14 rutas otra vez. Guarda de slug duplicado ejercitada en rojo (dos caballos con el mismo nombre → throw con mensaje que los nombra). `npm run lint:baseline` código 0 (4/4) y build re-corridos contra el árbol limpio post-commit. Sin tests (Seam A no acordado, decisión 8 del backlog): la verificación manual del 404 se repite en T-15. **No verificado:** navegador real vía MCP — la verificación fue por `curl` contra `next start`.
- 2026-08-30 · T-04 · Seam B: `src/data/imagen.ts` (`bucketDeRatio` por distancia logarítmica, `aspectRatioDeBucket`, `objectPositionDeFoto` con default `center`). Vitest agregado (`npm test`), `src/data/imagen.test.ts` con los 3 ciclos en rojo real uno a uno — el ciclo 2 (86 fotos vs `data/caballos.json`) obligó el paso de distancia lineal a logarítmica (2 fotos de ratio ~0.708 discrepaban). Capa 2: `/prueba-imagen` (noindex); DOM leído vía MCP → 86 elementos, `aspectRatio` computado de cada uno dentro de los 5 buckets, caja real coincide con el bucket esperado, 0 malos. `npm run build` en 0 (20 rutas), `npm run lint:baseline` 4/4, `npm test` 4/4 re-corridos contra árbol limpio post-commit. Commit `ac50219`. **No verificado:** `object-position` sobre `<img>` real (no hay fotos; `focus` en el dato es solo `center`) — se ejercita cuando T-11 traiga imágenes.
- 2026-08-30 · T-06 · sistema visual cerrado. `DESIGN.md` con los tokens y su porqué (tema único claro y fijo por la escena de PLAN §1.4; dials 7/4/3; color neutro frío + un acento `#2b5f6b` que no es latón/terracota/crema; Cabinet Grotesk Display + Satoshi por ADR-0002; escala tipográfica fluida corta; espaciado base 4px con `--space-section` fluida; **una sola** escala de radio `--radius: 10px`; movimiento dial 4 sin librería + `prefers-reduced-motion` global). Tokens en `src/app/globals.css` (mapeados en `@theme inline` para las utilidades de Tailwind v4). Espécimen en `src/app/muestra/page.tsx` (`/muestra`, `noindex`) que ejercita cada token — sin datos de caballos, los ejemplos se ven como ejemplos. Commit `0bd670b`. Verificado leyendo el DOM vía MCP contra `next dev`: escritorio 1440px (sin scroll horizontal, `--content-max` 1400, gutter 48, `--space-section` 160, h1 64px Cabinet Grotesk peso 700) y móvil real 390px con `emulate` (sin desborde, 0 elementos más anchos que el viewport, h1 42.7px, gutter 20, `--space-section` 99, botón 51px de alto); contrastes WCAG recalculados (muted 6.39:1, acento 6.5:1, blanco/acento 7.1:1 — todos ≥4.5). `npm run lint:baseline` código 0 (4/4). Portada y `/caballos/caballo-01` siguen en 200 tras el cambio de `globals.css`. **No verificado:** los hex de color contra fotos reales (no existen aún) — se afinan al llegar T0.1 sin tocar el resto; ninguna pantalla real del sitio usa todavía los tokens (empieza en T-07).
