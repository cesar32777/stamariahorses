# T-01 — Bootstrap del repo y despliegue vacío
Origen: PATRÓN
Fuente: scaffold de Next.js App Router + Tailwind v4 (docs oficiales)
Fase: A
Referencia: PLAN §2 Fase A · ADR-0001
Estado: móvil ⬜ · escritorio ⬜
Depende de: —
Animación: no
Tests: capa 1 → — · capa 2 → — · capa 3 → build + lint (fija el baseline)
Datos: n/a
Hecho cuando: una URL de Vercel responde 200 con una página que dice el nombre del sitio · `git log` tiene al menos un commit · verificado abriendo la URL, no leyendo la config

---

## Alcance

`git init`, `package.json`, Next.js App Router + TypeScript + Tailwind v4, ESLint, Prettier.
Fuentes con `next/font`, self-hosted (Cabinet Grotesk Display + Satoshi, ADR-0002).

## El límite que no se cruza

**El deploy a Vercel lo hace César** — necesita su cuenta. Deja el proyecto listo para desplegar
y dilo. **No intentes autenticarte.** Mientras tanto, la mitad del `Hecho cuando:` que depende de
la URL de Vercel queda declarada como no verificada, no se da por buena.

## El stack está propuesto, no confirmado

`PLAN-TICKETS.md` §2 lo propone; **este ticket es el que lo verifica en este entorno**. Si algo no
funciona, cámbialo y deja escrito por qué — en el cuerpo de este ticket, no en un chat.

## Fija el baseline del linter

Antes de cerrar: guarda el conteo del linter recién scaffoldeado como baseline en el repo.
A partir de aquí **el objetivo es el baseline, no el cero** — un scaffold llega con ofensas
heredadas y "no hay errores" no detectaría una regresión.
