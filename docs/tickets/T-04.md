# T-04 — Mapeo bucket → CSS y `focus` → `object-position`
Origen: PROPIO
Fuente: —
Fase: B
Referencia: PRODUCT.md RF11, RF13 · PLAN §3 (Seam B) · data/caballos.json
Estado: móvil ⬜ · escritorio ⬜
Depende de: T-02
Animación: no
Tests: capa 1 → **Seam B, el único acordado** (3 ciclos) · capa 2 → lectura del DOM de las 86 fotos
Datos: real
Hecho cuando: una página de prueba renderiza las 86 fotos con su bucket asignado y ninguna tiene `aspect-ratio` fuera de los 5 permitidos · verificado leyendo el DOM renderizado, no el código fuente

---

## Alcance

Los 5 buckets (`2:3 · 3:4 · 1:1 · 4:3 · 3:2`) a `aspect-ratio`. El campo `focus` de cada foto a
`object-position`, con `center` por defecto.

## Seam B — los tres ciclos, en este orden

| Ciclo | Test rojo | Implementación mínima |
|---|---|---|
| 1 | Una foto de ratio 1.4988 se asigna al bucket `3:2` | El mapeo al bucket más cercano |
| 2 | Ninguna de las 86 fotos sale con proporción fuera de los 5 buckets | El caso límite |
| 3 | Una foto sin `focus` rinde `center` | El default |

**Rojo antes que verde, uno a la vez.** El ciclo 2 se escribe **después** de que el 1 esté verde.
**Prohibido escribir los tres tests primero** (horizontal slicing).

## No recalcules el bucket del lado del test

`data/caballos.json` ya trae `bucket` y `recorte_pct` **medidos antes de que existiera el código de
render**. Ese archivo es el valor esperado. Derivarlo otra vez en el test con la misma fórmula que
el código bajo prueba **es tautológico y pasa por construcción**.
