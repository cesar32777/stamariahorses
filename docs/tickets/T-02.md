# T-02 — Tipado y validación del dato
Origen: PATRÓN
Fuente: Zod (o equivalente) en tiempo de build
Fase: A
Referencia: DOCUMENTO-FUNDACIONAL §4 (esquema confirmado) · CONTEXT.md
Estado: móvil ✅ · escritorio ✅
Depende de: T-01
Animación: no
Verificación: el build es la red
Datos: real
Hecho cuando: el build falla con mensaje legible si se le quita `nombre` a un caballo, y pasa con el archivo actual · probado corriendo el build con el archivo roto a propósito

---

## Alcance

Tipos `Caballo` y `Foto` en TypeScript que espejeen el esquema confirmado. Validación en tiempo de
build para que un `data/caballos.json` malformado **rompa el build** en vez de romper la página.

## Vocabulario

Los nombres de tipo y de campo salen de `CONTEXT.md`: Caballo, Catálogo, Ficha, Galería, Foto,
`bucket`, `focus`, Disponible/Retirado. **No "item", no "record", no "entity".**

## Nota — la restricción es la red

La validación de esquema en build es la red. Romper el archivo a propósito y ver el build fallar
**es** la verificación — hazla, no la supongas.

## Cómo quedó — verificado por ejecución (2026-08-30)

- **Zod instalado** (`zod@4.5.4`, dependencia de producción). No se justificó equivalente.
- `src/data/schema.ts` — esquemas `FotoSchema`, `CaballoSchema`, `VendedorSchema`, `CatalogoSchema`
  con los tipos inferidos `Foto`, `Caballo`, `Catalogo`. Campos y nombres de `CONTEXT.md` +
  DOCUMENTO-FUNDACIONAL §4: `nombre`/`estado`/`fotos` obligatorios y no nulos; `sexo` `nacimiento`
  `raza` `capa` `alzada` `descripcion` nulos permitidos (RF5). `id` se valida (hoy está en cada
  registro y nombra al caballo en el error) aunque sea accidente del PDF.
- `src/data/caballos.ts` — **el punto de enganche.** Importa `data/caballos.json`, hace
  `CatalogoSchema.safeParse` al evaluarse el módulo y `throw` con mensaje legible si falla. Se
  importa desde `src/app/page.tsx` (que además consume `caballos` para que no sea import muerto),
  así que `next build` lo ejecuta en "Collecting page data".
- **Verificación por `next build` (el build es la red):**
  - Quitado `nombre` al caballo `id 07` → `npm run build` **falla, código 1**:
    `caballo id 07 → campo 'nombre': Invalid input: expected string, received undefined`.
  - `bucket: "16:9"` en la 3ª foto del caballo 5 → **falla, código 1**:
    `caballo Caballo 05 → campo 'fotos.2.bucket': Invalid option: expected one of "2:3"|...`.
    Confirma que el mensaje nombra al caballo y la ruta del campo también en el caso anidado.
  - Restaurado cada vez con `git checkout -- data/caballos.json` (JSON válido, `git status`
    limpio) → `npm run build` **pasa, código 0**. El JSON roto nunca se commiteó.
- **Cada restricción del esquema ejercitada en rojo, de a una** (CLAUDE.md: *"¿qué escenario la
  pondría en rojo?"*). 19 mutaciones contra `CatalogoSchema.safeParse` — 17 rotas + 2 controles
  positivos (`sexo: null`, archivo intacto) —, todas con el resultado esperado:
  enums (`estado`, `sexo`, `bucket`, `orient`, `recorte_eje`), `""` vs `min(1)`, tipo equivocado
  (`alzada` string, `hero` string, `telefono` number), `int` no entero, arrays vacíos
  (`fotos`, `caballos`), positivos (`w`), y campos/objetos faltantes (`archivo`, `vendedor`).
  Script desechable en scratchpad, no commiteado.
- `npm run lint:baseline` → código 0, `4 errores (baseline 4)` sin cambios.
